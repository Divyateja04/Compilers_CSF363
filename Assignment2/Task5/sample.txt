program ArrayAverage;
var
  numbers: array[1..5] of Integer;
  i, a, b, sum, d : Integer;
  average : real;
  c: char;
begin
  i := 100;
  i := i + i * (i - i) + (i - i) * d;
  x := (7 < 5) and (6 > 5);
  a := -3;

  write("Enter 5 integer values:");

  c := 'c';
  write(c);

  for i := 1 to 5 do
  begin
    read(numbers[i]);
  end;
end.